# Автоматическая торговля на предикшн маркете Boink

Софт для автоматической торговли на предикшн маркете https://prediction.boinknfts.club

## Возможности

- ✅ Автоматическое размещение ставок на рандомных маркетах
- ✅ Автоматический клейм ежедневных наград
- ✅ Автоматический клейм фаусетов (Circle и Inkonchain)
- ✅ Решение hCaptcha через sctg.xyz API
- ✅ Поддержка множественных кошельков
- ✅ Поддержка прокси для каждого кошелька
- ✅ Анализ рынка для принятия решений
- ✅ Настраиваемые параметры торговли
- ✅ Логирование всех операций
- ✅ Статистика торговли

## Установка

1. Установите Python 3.8 или выше

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файлы с кошельками и прокси:
   - `wallets.txt` - список адресов кошельков (один на строку)
   - `proxies.txt` - список прокси (один на строку, опционально)
   - `.env` - настройки программы

4. Заполните файлы:
   - В `wallets.txt` укажите адреса кошельков (формат: `0x...`)
   - В `private_keys.txt` укажите приватные ключи (формат: hex строка)
   - В `proxies.txt` укажите прокси (формат: `host:port` или `user:pass@host:port`)
   - Все связывается по порядку (первая строка = первый кошелек)

## Настройка

Отредактируйте файл `.env` для настройки параметров:

### Файлы с кошельками, ключами и прокси:
- `private_keys.txt` - **ОБЯЗАТЕЛЬНО** - список приватных ключей (один на строку)
  - Формат: hex строка (64 символа, с префиксом `0x` или без)
  - Пример: `0x1234...` или `1234...`
  - **Адреса кошельков извлекаются автоматически из приватных ключей!**
  - Ключи связываются с прокси по порядку (первая строка ключа = первый прокси)
- `wallets.txt` - **ОПЦИОНАЛЬНО** - список адресов кошельков (один на строку)
  - Используется только для проверки соответствия (если указан)
  - Если не указан, адреса извлекаются только из приватных ключей
  - Формат: `0x...` (42 символа)
  - Пустые строки и комментарии (начинающиеся с `#`) игнорируются
- `proxies.txt` - опционально - список прокси (один на строку)
  - Формат 1: `host:port`
  - Формат 2: `user:pass@host:port`
  - Прокси связываются с кошельками по порядку (первая строка прокси = первый кошелек)
  - Если прокси меньше чем кошельков, оставшиеся кошельки работают без прокси

### Настройки торговли:
- `MIN_BET_AMOUNT` - минимальная сумма ставки (по умолчанию 0.01)
- `MAX_BET_AMOUNT` - максимальная сумма ставки (по умолчанию 1.0)
- `BET_INTERVAL_SECONDS` - интервал между ставками в секундах (по умолчанию 60)
- `RANDOM_MARKETS` - выбирать рандомные маркеты (true/false, по умолчанию true)

### Настройки автоматизации:
- `AUTO_CLAIM_DAILY` - автоматически клеймить дейлик при старте (true/false, по умолчанию true)
- `AUTO_CLAIM_FAUCETS` - автоматически клеймить фаусеты при старте (true/false, по умолчанию true)
- `AUTO_BET_ENABLED` - включить автоматические ставки (true/false, по умолчанию true)

### Настройки фаусетов:
- `CAPTCHA_API_KEY` - API ключ для sctg.xyz (необязательно, но требуется для Inkonchain фаусета)

### Настройки логирования:
- `LOG_LEVEL` - уровень логирования (DEBUG, INFO, WARNING, ERROR, по умолчанию INFO)

## Использование

Запустите программу:
```bash
python main.py
```

Программа выполнит следующие шаги для **каждого кошелька**:

1. **Клейм ежедневной награды** (если `AUTO_CLAIM_DAILY=true`)
   - Проверяет, не собрана ли уже награда
   - Клеймит если доступно

2. **Клейм фаусетов** (если `AUTO_CLAIM_FAUCETS=true`)
   - Circle фаусет (https://faucet.circle.com)
   - Inkonchain Mystery фаусет (https://inkonchain.com) - требует hCaptcha решение

3. **Автоматическая торговля**
   - Работает со всеми кошельками параллельно
   - Выбирает рандомные маркеты (если `RANDOM_MARKETS=true`)
   - Анализирует рынок или делает случайный выбор
   - Размещает ставки с настраиваемым интервалом
   - Использует случайные суммы в указанном диапазоне
   - Каждый кошелек использует свой прокси (если указан)

Для остановки нажмите `Ctrl+C`

## Настройка файлов

### private_keys.txt
**ОБЯЗАТЕЛЬНО:** Создайте файл `private_keys.txt` и укажите приватные ключи:
```
0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890
0xYourThirdPrivateKeyHere
```

**ВАЖНО:**
- **Адреса кошельков извлекаются автоматически из приватных ключей!**
- Вам не нужно указывать адреса в `wallets.txt` (но можно для проверки)
- Первая строка ключа = первый кошелек, вторая = второй и т.д.
- **НИКОГДА не коммитьте `private_keys.txt` в git!**
- Храните файл в безопасном месте
- Используйте только тестовые ключи для тестирования

### wallets.txt (опционально)
Если хотите проверить соответствие, можете создать файл `wallets.txt` с адресами:
```
0x595C33b009D4B4d0301626F2a5b0421283462136
0x7deD23b9fA7Da2Cbb0aEF0e56661De35b3a826E9
0xYourThirdWalletAddressHere
```

Программа автоматически проверит, что адреса из ключей совпадают с адресами в файле (если он указан).

**Примечание:** Формат подписи сообщений может отличаться в зависимости от требований API. Если запросы не проходят, возможно потребуется уточнить формат сообщения для подписи в файле `api_client.py` (методы `make_bet` и `claim_daily`).

### proxies.txt (опционально)
Создайте файл `proxies.txt` и укажите прокси:
```
127.0.0.1:8080
user:password@proxy.example.com:3128
192.168.1.1:1080
```

**Важно:** Прокси связываются с кошельками по порядку. Первая строка прокси используется для первого кошелька, вторая для второго и т.д.

### Настройка sctg.xyz для hCaptcha

Для работы с Inkonchain фаусетом требуется решение hCaptcha. Софт использует sctg.xyz API:

1. Получите API ключ для сервиса sctg.xyz
2. Укажите его в `.env` файле: `CAPTCHA_API_KEY=your_api_key`

**Примечание:** Без API ключа Inkonchain фаусет не будет работать, но Circle фаусет и торговля будут работать нормально.

## Стратегия торговли

Текущая стратегия:
- 70% времени анализирует последние ставки на маркете и выбирает исход противоположный большинству
- 30% времени делает случайный выбор
- При ошибке анализа делает случайный выбор

Вы можете изменить логику в файле `trader.py` в методе `analyze_market()` и `make_bet_with_strategy()`.

## Логи

Все операции логируются в файл `trader.log` с временными метками и выводятся в консоль с цветовой индикацией:
- 🔵 INFO - информационные сообщения
- 🟢 SUCCESS - успешные операции
- 🟡 WARNING - предупреждения
- 🔴 ERROR - ошибки

## Статистика

Программа выводит статистику каждые 10 ставок:
- Всего ставок
- Успешных/неудачных ставок
- Количество клеймов дейлика
- Количество клеймов фаусетов
- Количество доступных маркетов

## Внимание

⚠️ **Этот софт работает с реальными деньгами в тестнете. Используйте на свой риск!**

- Убедитесь, что правильно настроили параметры перед запуском
- Начните с небольших сумм для тестирования
- Мониторьте работу программы
- Проверьте баланс перед запуском
- API ключ sctg.xyz может стоить денег - используйте разумно

## Структура проекта

```
boinkpredict/
├── main.py              # Главный файл запуска
├── trader.py            # Основная логика торговли
├── api_client.py        # API клиент для работы с маркетом
├── faucet_client.py     # Клиент для работы с фаусетами
├── wallet_manager.py    # Менеджер кошельков и прокси
├── config.py            # Конфигурация
├── requirements.txt     # Зависимости Python
├── test_api.py          # Тестовый скрипт для проверки API
├── wallets.txt          # Список кошельков (ОБЯЗАТЕЛЬНО)
├── proxies.txt          # Список прокси (опционально)
├── .env                 # Настройки (создайте вручную)
├── .gitignore           # Игнорируемые файлы
└── README.md            # Документация
```

## API Endpoints

Программа использует следующие API endpoints:

### Предикшн маркет:
- `POST /api/user/bet` - размещение ставки
- `GET /api/user/{address}/bets` - получение ставок пользователя
- `GET /api/market/{id}/bets` - получение ставок маркета
- `POST /api/user/{address}/claim-daily` - клейм ежедневной награды

### Фаусеты:
- `POST https://faucet.circle.com/api/graphql` - Circle фаусет (GraphQL)
- `POST https://mystery-faucet.inkonchain.com/api/claim` - Inkonchain фаусет (требует hCaptcha)

## Устранение неполадок

### Ошибка "Не найдено ни одного кошелька"
- Убедитесь, что создали файл `wallets.txt` и указали в нем адреса кошельков
- Проверьте формат адресов (должны начинаться с `0x` и быть длиной 42 символа)
- Убедитесь, что файл не пустой и содержит валидные адреса

### Фаусеты не клеймятся
- Проверьте, что указали правильный адрес кошелька
- Для Inkonchain фаусета требуется `CAPTCHA_API_KEY`
- Убедитесь, что на балансе 2captcha есть средства

### Ставки не размещаются
- Проверьте баланс кошелька
- Убедитесь, что маркет доступен
- Проверьте интервал между ставками (`BET_INTERVAL_SECONDS`)

### Ошибки API
- Проверьте интернет-соединение
- Убедитесь, что API доступен
- Проверьте логи в файле `trader.log`
